-----------0. mouse scroll  ->  zoom X (всех графиков), (при этом будет очень круто если учитывать положение курсора на смещение диапазона)

-----------1. ctrl + mouse scroll -> zoom Y ( графика над которым курсор)

2. shift + mouse scroll = L click + drag -> сдвиг всех графиков по X

-----------3. в списке графиков: дабл клик - нарисовать график, а ренэйм по ф2(уже есть) или прКлик меню

-----------4. цвет фона

-----------5. цвета линий

-----------4.CSV: изменить порядок установления тпа данных по оси Х
            например:
            открыть файл --- читаем файл в буфер и отображаем данные из хедеров и закрываем файл
            далее указываем какой столбец считать временем и в каком формате, а также какие столбцы нужны
            переписываем данные в нужном формате

5.DB:
предлагаю попробовать следующее::
посмкольку есть primary key "n", то
делаем запрос мин и макс значений, также можно дейт тайм попробовать на этом этапе считать для крайних записей.
отображаем в таблице какие есть параметры, сколько записей у каждого и какие даты от и до
выбираем парамеры для просмотра
далее разьиваем запросы на чтение по (например 10000 строк) SELECT ***** WHERE N от 0 до 10k, 10k - 20k и тд
можно и прогресс бар сделать
и такими малыми порциями можно вычитать большой обьем без таймаута и перезапросить порцию можно..